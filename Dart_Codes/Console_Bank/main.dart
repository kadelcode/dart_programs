class Bank
{
  String acc_name= ""; // account name
  int acc_num = 0; // account number
  int acc_bal = 0; // account balance
  // using the constructor function to initialize variables
   Bank(String acc_name, int acc_num, int acc_bal)
  {
    this.acc_name = acc_name;
    this.acc_num = acc_num;
    this.acc_bal = acc_bal;

  }
  
  // class methods
}

int main()
{
  // object of class(Bank) here
  return (0);
}